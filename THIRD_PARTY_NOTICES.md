# Third-party notices

This file separates this repository's own project materials from external components.

The repository-level `LICENSE` and `NOTICE.txt` apply to LiteRaceSegNet-related code, documentation, diagrams, experiment notes, configuration files, and scripts authored by Kim Wonseok (김원석) for this portfolio project.

They do not relicense external datasets, pretrained weights, third-party model architectures, downloaded Hugging Face files, APIs, Python packages, or any asset not authored for this repository.

## Own project materials

The following are provided only for portfolio and academic demonstration purposes, unless Kim Wonseok (김원석) grants separate written permission:

- LiteRaceSegNet implementation under `seg/core/`
- training, inference, comparison, and evidence-building scripts written for this project
- wrapper code that connects the project pipeline
- documentation written for this repository
- architecture diagrams authored for this project
- placeholder dataset layout files and run scripts

No permission is granted to copy, redistribute, modify, publish, sublicense, create derivative works from, or commercially use these project materials without prior permission from Kim Wonseok (김원석).

## External Python packages

Installed by requirements files. They are not vendored into this repository.

Main packages:

- PyTorch
- TorchVision
- OpenCV Python
- NumPy
- PyYAML
- Pillow
- tqdm
- Hugging Face Transformers, optional
- safetensors, optional
- OpenAI Python package, optional for the LLM explanation path

Users must follow each package's own license.

## SegFormer / Transformer baseline

SegFormer-B3 is used as a comparison baseline through a local adapter and the Hugging Face Transformers interface.

This repository does not include:

- the official NVLabs SegFormer source repository
- pretrained SegFormer weights
- Hugging Face cached model files
- fine-tuned SegFormer checkpoints

The optional setup script may download/cache a Hugging Face model in the user's local environment. Those downloaded files are external assets and must not be committed unless their original terms explicitly allow redistribution.

This repository's own copyright notice applies only to this project's wrapper, adapter, training, evaluation, and service code authored by Kim Wonseok (김원석). It does not apply to SegFormer itself or pretrained model weights.

## TorchVision models

Some baseline code may use TorchVision model definitions and optionally TorchVision-provided pretrained weights.

TorchVision code and weights remain under their own license and terms. If pretrained weights are downloaded locally, do not commit them to this repository.

## Datasets

Dataset files are not included.

The directories under `datasets/` only define the expected folder structure. Users must prepare permitted image-mask pairs and follow the license terms of the dataset source.

Do not commit:

- raw dataset images
- segmentation masks or labels
- processed dataset copies
- private road images
- paid or restricted datasets
- generated checkpoints trained from restricted datasets unless redistribution is allowed

## Generated artifacts

Generated artifacts are local evidence files and are excluded from version control by `.gitignore`.

Examples:

- checkpoints: `.pt`, `.pth`, `.ckpt`, `.safetensors`, `.bin`
- overlays and masks generated by inference
- CSV/JSON metric reports
- logs and experiment folders
- local Hugging Face cache folders
- thesis DOCX/PDF drafts

## API keys and online services

The optional LLM service reads API keys from environment variables. No key is included in this repository.

Never commit:

- `.env`
- API keys
- cloud credentials
- private endpoint URLs
- billing-related configuration

## Commercial use note

This repository is public for portfolio viewing and academic demonstration. That does not grant permission to reuse this repository's own LiteRaceSegNet code, documentation, diagrams, experiment notes, configuration files, or scripts authored by Kim Wonseok (김원석).

External packages, datasets, model weights, APIs, and references remain under their own licenses and terms.
